---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "commander_new_birth_certificate Data Source - commander"
subcategory: "New (NSF) Records"
description: |-
  Use this data source to look up a New (NSF) Birth Certificate record by UID.
---

# commander_new_birth_certificate (Data Source)

Use this data source to look up a **New (NSF) Birth Certificate** record by **UID**.

## Example Usage

```terraform
# Look up a Birth Certificate record by title or UID.
data "commander_new_birth_certificate" "example" {
  birth_certificate = "_REPLACE_WITH_RECORD_TITLE_OR_UID_"
}

output "birth_certificate_id" {
  value = data.commander_new_birth_certificate.example.id
}

output "birth_certificate_title" {
  value = data.commander_new_birth_certificate.example.title
}

output "birth_certificate_notes" {
  value = data.commander_new_birth_certificate.example.notes
}

output "birth_certificate_folder_location" {
  value = data.commander_new_birth_certificate.example.folder_location
}

output "birth_certificate_name" {
  value = data.commander_new_birth_certificate.example.name
}

output "birth_certificate_birth_date" {
  value = data.commander_new_birth_certificate.example.birth_date
}

output "birth_certificate_custom" {
  value = data.commander_new_birth_certificate.example.custom
}

output "birth_certificate_share" {
  value = data.commander_new_birth_certificate.example.share
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `birth_certificate` (String) New (NSF) Birth Certificate record **title** or **UID** to look up.

### Read-Only

- `birth_date` (String) Date of **birth** (`YYYY-MM-DD`).
- `custom` (Attributes List) Custom fields stored in the record's `custom` array. (see [below for nested schema](#nestedatt--custom))
- `folder_location` (String) Folder `path` or `UID` where the record is to be stored.
- `id` (String) Unique identifier (UID) of the vault record.
- `name` (Attributes) Person name (`name` field): first, middle, last. (see [below for nested schema](#nestedatt--name))
- `notes` (String) Note of the record.
- `share` (Map of String) Mapping of share permissions for this folder or record. For folders, keys can identify either users (**UID** or **email**) or teams (**UID** or **name**). For records, keys can identify users only (**UID** or **email**). Values specify the permission level: `viewer`, `share-manager`, `content-manager`, `content-share-manager`, or `full-manager`. The owner is implicit and never present in this map; the API rejects owner entries.
- `title` (String) Record title.

<a id="nestedatt--custom"></a>
### Nested Schema for `custom`

Read-Only:

- `label` (String) Field label.
- `sensitive` (Boolean) Whether the value should be treated as **sensitive**.
- `type` (String) Keeper field **type** (e.g. `text`, `email`, `secret`).
- `value` (String, Sensitive) Field **value** (JSON-encoded for complex types).


<a id="nestedatt--name"></a>
### Nested Schema for `name`

Read-Only:

- `first` (String) **First name**.
- `last` (String) **Last name**.
- `middle` (String) **Middle name**.
