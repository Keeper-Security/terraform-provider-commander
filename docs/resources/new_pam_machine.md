---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "commander_new_pam_machine Resource - commander"
subcategory: ""
description: |-
  Creates and manages a Nested-Shared (new) PAM machine record with PAM settings and share permissions in your Keeper vault.
  For more information, see the PAM Machine documentation https://docs.keeper.io/en/keeperpam/privileged-access-manager/getting-started/pam-resources/pam-machine.
---

# commander_new_pam_machine (Resource)

Creates and manages a **Nested-Shared (new) PAM machine record** with **PAM settings** and **share permissions** in your Keeper vault.

For more information, see the [PAM Machine documentation](https://docs.keeper.io/en/keeperpam/privileged-access-manager/getting-started/pam-resources/pam-machine).

## Example Usage

```terraform
# ------------------------------------------------------------------
# Example 1 - Minimal: only required fields (title + hostname)
# ------------------------------------------------------------------
resource "commander_new_pam_machine" "minimal" {
  title = "PAM Machine - bastion-01"

  hostname_or_ip = {
    hostname            = "bastion-01.corp.example.com"
    administrative_port = 22
  }
}

# ------------------------------------------------------------------
# Example 2 - SSH machine with full PAM settings + share permissions
# ------------------------------------------------------------------
resource "commander_new_pam_machine" "example" {
  title = "PAM Machine - prod-server-01"

  hostname_or_ip = {
    hostname            = "prod-server-01.corp.example.com"
    administrative_port = 22
  }

  operating_system = "Ubuntu 22.04 LTS"
  instance_name    = "prod-server-01"
  instance_id      = "i-0abc123def456"
  provider_group   = "AWS"
  provider_region  = "us-east-1"
  notes            = "Primary production server."
  folder_location  = "_REPLACE_WITH_NSF_FOLDER_UID_"

  # ----------------------------------------------------------------
  # Per-user share permissions (optional).
  # Map key = user email; value = one of:
  #   viewer, share-manager, content-manager,
  #   content-share-manager, full-manager.
  # ----------------------------------------------------------------
  share = {
    "alice@example.com" = "full-manager"
    "bob@example.com"   = "content-manager"
    "carol@example.com" = "viewer"
  }

  pam_settings {
    configuration              = "_REPLACE_WITH_PAM_CONFIGURATION_UID_"
    administrative_credentials = "_REPLACE_WITH_ADMIN_CREDENTIAL_UID_"
    allow_supply_host          = false

    tunnel {
      enable                   = true
      remote_target_port       = 22
      re_use_port              = true
      use_specified_local_port = true
      local_port               = 10022
    }

    connection {
      enable            = true
      protocol          = "ssh" # one of: kubernetes, rdp, ssh, telnet, vnc
      connection_port   = 22
      launch_credential = "_REPLACE_WITH_LAUNCH_CREDENTIAL_UID_"

      # Only the block matching "protocol" should be set; see the
      # commander_classic_pam_machine example for connection variants
      # (kubernetes, rdp, ssh, telnet, vnc).
      ssh {
        session_recording      = true
        typescript_recording   = true
        recording_include_keys = true
        allow_supply_user      = true
        read_only              = false
        color_scheme           = "black-white"
        font_name              = "monospace"
        font_size              = 12
        scrollback             = 2000
        backspace              = "127"
        terminal_type          = "xterm-256color"
        locale                 = "en_US.UTF-8"
        timezone               = "America/New_York"
        server_alive_interval  = 60
        command                = "/bin/bash"
        disable_copy           = false
        disable_paste          = false
        host_key               = "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIExampleHostKey..."

        sftp {
          enable_sftp = true
        }
      }
    }
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `hostname_or_ip` (Attributes) **Hostname or IP address** with an optional port for the PAM machine. (see [below for nested schema](#nestedatt--hostname_or_ip))
- `title` (String) **Title** of the PAM machine record.

### Optional

- `folder_location` (String) Folder **UID** or path to store PAM machine record in your Keeper vault. If not provided, the record will be stored in the root path of vault.
- `instance_id` (String) **Azure or AWS Instance ID**
- `instance_name` (String) **Azure or AWS Instance Name**
- `notes` (String) **Notes** for this PAM machine record.
- `operating_system` (String) **The target's Operating System**
- `pam_settings` (Block, Optional) **PAM settings** for the record, including connection, tunnel, and administrative options. (see [below for nested schema](#nestedblock--pam_settings))
- `provider_group` (String) **Provider group** of the PAM machine.
- `provider_region` (String) **AWS region** of hosted directory.
- `share` (Map of String) Mapping of share permissions for this folder or record. For folders, keys can identify either users (**UID** or **email**) or teams (**UID** or **name**). For records, keys can identify users only (**UID** or **email**). Values specify the permission level: `viewer`, `share-manager`, `content-manager`, `content-share-manager`, or `full-manager`. Owner is implicit and never appears in this map.

### Read-Only

- `id` (String) The PAM machine record **UID** assigned by Keeper after create.

<a id="nestedatt--hostname_or_ip"></a>
### Nested Schema for `hostname_or_ip`

Required:

- `hostname` (String) **Address of the machine resource**.

Optional:

- `administrative_port` (Number) **Port to connect on. The Gateway uses this to determine connection method.**


<a id="nestedblock--pam_settings"></a>
### Nested Schema for `pam_settings`

Optional:

- `administrative_credentials` (String) **Linked PAM User credential** used for connection and administrative operations.
- `allow_supply_host` (Boolean) Whether the PAM record allows supplying a host at connection time.
- `configuration` (String) **Configuration** identifier for the PAM record.
- `connection` (Block, Optional) **Connection** settings for the PAM record. (see [below for nested schema](#nestedblock--pam_settings--connection))
- `tunnel` (Block, Optional) **Tunneling** (port-forward) settings for the PAM record. (see [below for nested schema](#nestedblock--pam_settings--tunnel))

<a id="nestedblock--pam_settings--connection"></a>
### Nested Schema for `pam_settings.connection`

Optional:

- `connection_port` (Number) **Connection port**. Only applicable when connection is enabled.
- `enable` (Boolean) Whether the **connection** is enabled for this PAM record.
- `kubernetes` (Block, Optional) **Kubernetes** protocol-specific connection attributes. (see [below for nested schema](#nestedblock--pam_settings--connection--kubernetes))
- `launch_credential` (String) **Launch credential**. Only applicable when connection is enabled.
- `protocol` (String) **Connection protocol**. Must be one of: `kubernetes`, `rdp`, `ssh`, `telnet`, `vnc`.
- `rdp` (Block, Optional) **RDP** protocol-specific connection attributes. (see [below for nested schema](#nestedblock--pam_settings--connection--rdp))
- `ssh` (Block, Optional) **SSH** protocol-specific connection attributes. (see [below for nested schema](#nestedblock--pam_settings--connection--ssh))
- `telnet` (Block, Optional) **Telnet** protocol-specific connection attributes. (see [below for nested schema](#nestedblock--pam_settings--connection--telnet))
- `vnc` (Block, Optional) **VNC** protocol-specific connection attributes. (see [below for nested schema](#nestedblock--pam_settings--connection--vnc))

<a id="nestedblock--pam_settings--connection--kubernetes"></a>
### Nested Schema for `pam_settings.connection.kubernetes`

Optional:

- `allow_supply_user` (Boolean) Allow users to **select credentials** from their vault.
- `backspace` (String) ASCII code sent by the **backspace** key. Must be `127` (DEL, default) or `8` (Ctrl+H).
- `ca_cert` (String) **Certificate Authority certificate** for the Kubernetes connection.
- `client_cert` (String) **Client certificate** for the Kubernetes connection.
- `client_key` (String) **Client key** for the Kubernetes connection.
- `color_scheme` (String) **Color scheme** for the connection terminal. Accepts named presets (`black-white`, `gray-black`, `green-black`, `white-black`) or a Guacamole terminal color scheme string.
- `command` (String) **Execute command** for the Kubernetes connection.
- `container` (String) **Container name** for the Kubernetes connection.
- `font_name` (String) **Font name** for the connection terminal.
- `font_size` (Number) **Font size** for the connection terminal.
- `ignore_cert` (Boolean) **Ignore server certificate** for the Kubernetes connection.
- `namespace` (String) Container **namespace** for the Kubernetes connection.
- `pod` (String) **Pod name** for the Kubernetes connection.
- `read_only` (Boolean) Whether the connection is **read-only**.
- `recording_include_keys` (Boolean) Whether to include **keystrokes** in the recording.
- `rotate_on_termination` (Boolean) **Rotate** launch credentials upon session termination.
- `scrollback` (Number) **Maximum scrollable size** for the connection terminal.
- `session_recording` (Boolean) Whether **session recording** is enabled.
- `typescript_recording` (Boolean) Whether **typescript recording** is enabled.
- `use_ssl` (Boolean) Use **SSL/TLS** for the Kubernetes connection.


<a id="nestedblock--pam_settings--connection--rdp"></a>
### Nested Schema for `pam_settings.connection.rdp`

Optional:

- `allow_supply_user` (Boolean) Allow users to **select credentials** from their vault.
- `client_name` (String) **Client name** for the RDP connection.
- `color_depth` (Number) **Color depth** in bits per pixel for the RDP connection (`8`, `16`, `24`, or `32`).
- `console` (Boolean) Use **console session** for the RDP connection.
- `console_audio` (Boolean) Enable **console audio** for the RDP connection.
- `disable_audio` (Boolean) Disable **audio** for the RDP connection.
- `disable_auth` (Boolean) Disable **authentication** for the RDP connection.
- `disable_bitmap_caching` (Boolean) Disable **bitmap caching** for the RDP connection.
- `disable_copy` (Boolean) Whether **copy** is disabled for the connection.
- `disable_glyph_caching` (Boolean) Disable **glyph caching** for the RDP connection.
- `disable_offscreen_caching` (Boolean) Disable **offscreen caching** for the RDP connection.
- `disable_paste` (Boolean) Whether **paste** is disabled for the connection.
- `dpi` (Number) **DPI** for the RDP connection display.
- `drive_redirection_mode` (String) **Drive redirection mode** for the RDP connection (e.g. `none`, `user`, `resource`).
- `enable_audio_input` (Boolean) Enable **audio input** for the RDP connection.
- `enable_desktop_composition` (Boolean) Enable **desktop composition** during RDP sessions.
- `enable_font_smoothing` (Boolean) Enable **font smoothing** during RDP sessions.
- `enable_full_window_drag` (Boolean) Enable **full window drag** during RDP sessions.
- `enable_menu_animations` (Boolean) Enable **menu animations** during RDP sessions.
- `enable_printing` (Boolean) Enable **printing** for the RDP connection.
- `enable_theming` (Boolean) Enable **theming** during RDP sessions.
- `enable_touch` (Boolean) Enable **touch input** for the RDP connection.
- `enable_wallpaper` (Boolean) Enable **wallpaper display** during RDP sessions.
- `force_lossless` (Boolean) Force **lossless compression** for the RDP connection.
- `height` (Number) **Height** in pixels for the RDP connection display.
- `ignore_cert` (Boolean) **Ignore server certificate** for the RDP connection.
- `initial_program` (String) **Initial program** to run on RDP connect.
- `load_balance_info` (String) **Load balance info** for the RDP connection.
- `normalize_clipboard` (String) **Clipboard normalization** mode for the RDP connection (e.g. `preserve`).
- `preconnection_blob` (String) **Pre-connection blob** for the RDP connection.
- `preconnection_id` (String) **Pre-connection ID** for the RDP connection.
- `read_only` (Boolean) Whether the connection is **read-only**.
- `recording_include_keys` (Boolean) Whether to include **keystrokes** in the recording.
- `redirected_printer_name` (String) **Redirected printer name** for the RDP connection.
- `remote_app` (String) **Remote application** to launch.
- `remote_app_args` (String) **Arguments** for the remote application.
- `remote_app_dir` (String) **Working directory** for the remote application.
- `resize_method` (String) **Resize method** for the RDP connection (e.g. `display-update`, `reconnect`).
- `rotate_on_termination` (Boolean) **Rotate** launch credentials upon session termination.
- `security` (String) **Security mode** for the RDP connection (e.g. `any`, `nla`, `tls`, `rdp`).
- `server_layout` (String) **Keyboard layout** for the RDP connection (e.g. `en-us-qwerty`, `de-de-qwertz`).
- `session_recording` (Boolean) Whether **session recording** is enabled.
- `sftp` (Block, Optional) **SFTP** settings for the RDP connection. (see [below for nested schema](#nestedblock--pam_settings--connection--rdp--sftp))
- `timezone` (String) **Timezone** for the RDP connection.
- `width` (Number) **Width** in pixels for the RDP connection display.

<a id="nestedblock--pam_settings--connection--rdp--sftp"></a>
### Nested Schema for `pam_settings.connection.rdp.sftp`

Optional:

- `enable_sftp` (Boolean) Whether **SFTP** is enabled.
- `sftp_directory` (String) **SFTP root directory**.
- `sftp_resource_uid` (String) UID of the **SFTP resource** record.
- `sftp_server_alive_interval` (Number) **SFTP server alive interval** in seconds.
- `sftp_user_uid` (String) UID of the **SFTP user** record.



<a id="nestedblock--pam_settings--connection--ssh"></a>
### Nested Schema for `pam_settings.connection.ssh`

Optional:

- `allow_supply_user` (Boolean) Allow users to **select credentials** from their vault.
- `backspace` (String) ASCII code sent by the **backspace** key. Must be `127` (DEL, default) or `8` (Ctrl+H).
- `color_scheme` (String) **Color scheme** for the connection terminal. Accepts named presets (`black-white`, `gray-black`, `green-black`, `white-black`) or a Guacamole terminal color scheme string.
- `command` (String) **Command** to execute on the SSH server after connecting.
- `disable_copy` (Boolean) Whether **copy** is disabled for the connection.
- `disable_paste` (Boolean) Whether **paste** is disabled for the connection.
- `font_name` (String) **Font name** for the connection terminal.
- `font_size` (Number) **Font size** for the connection terminal.
- `host_key` (String) **Known host public key** for the SSH connection.
- `locale` (String) **Locale** for the SSH session. Use `$LANG` (default) to inherit client locale, or any valid POSIX locale string (e.g. `en_US.UTF-8`, `fr_FR.UTF-8`).
- `read_only` (Boolean) Whether the connection is **read-only**.
- `recording_include_keys` (Boolean) Whether to include **keystrokes** in the recording.
- `rotate_on_termination` (Boolean) **Rotate** launch credentials upon session termination.
- `scrollback` (Number) **Maximum scrollable size** for the connection terminal.
- `server_alive_interval` (Number) Interval in seconds between **keep-alive messages** sent to the SSH server.
- `session_recording` (Boolean) Whether **session recording** is enabled.
- `sftp` (Block, Optional) **SFTP** settings for the SSH connection. (see [below for nested schema](#nestedblock--pam_settings--connection--ssh--sftp))
- `terminal_type` (String) **Terminal emulator type** string for the SSH session (e.g. `xterm`, `vt100`).
- `timezone` (String) **Timezone** for the SSH session. Use `$TZ` (default) to inherit client timezone, or any valid IANA timezone (e.g. `America/New_York`, `Europe/London`). See [IANA timezone list](https://en.wikipedia.org/wiki/List_of_tz_database_time_zones).
- `typescript_recording` (Boolean) Whether **typescript recording** is enabled.

<a id="nestedblock--pam_settings--connection--ssh--sftp"></a>
### Nested Schema for `pam_settings.connection.ssh.sftp`

Optional:

- `enable_sftp` (Boolean) Whether **SFTP** is enabled for the SSH connection.



<a id="nestedblock--pam_settings--connection--telnet"></a>
### Nested Schema for `pam_settings.connection.telnet`

Optional:

- `allow_supply_user` (Boolean) Allow users to **select credentials** from their vault.
- `backspace` (String) ASCII code sent by the **backspace** key. Must be `127` (DEL, default) or `8` (Ctrl+H).
- `color_scheme` (String) **Color scheme** for the connection terminal. Accepts named presets (`black-white`, `gray-black`, `green-black`, `white-black`) or a Guacamole terminal color scheme string.
- `disable_copy` (Boolean) Whether **copy** is disabled for the connection.
- `disable_paste` (Boolean) Whether **paste** is disabled for the connection.
- `font_name` (String) **Font name** for the connection terminal.
- `font_size` (Number) **Font size** for the connection terminal.
- `login_failure_regex` (String) Regular expression to detect a **failed login** during Telnet login.
- `login_success_regex` (String) Regular expression to detect a **successful login** during Telnet login.
- `password_regex` (String) Regular expression to detect the **password prompt** during Telnet login.
- `read_only` (Boolean) Whether the connection is **read-only**.
- `recording_include_keys` (Boolean) Whether to include **keystrokes** in the recording.
- `rotate_on_termination` (Boolean) **Rotate** launch credentials upon session termination.
- `scrollback` (Number) **Maximum scrollable size** for the connection terminal.
- `session_recording` (Boolean) Whether **session recording** is enabled.
- `terminal_type` (String) **Terminal emulator type** string for the Telnet session (e.g. `xterm`, `vt100`).
- `typescript_recording` (Boolean) Whether **typescript recording** is enabled.
- `username_regex` (String) Regular expression to detect the **username prompt** during Telnet login.


<a id="nestedblock--pam_settings--connection--vnc"></a>
### Nested Schema for `pam_settings.connection.vnc`

Optional:

- `allow_supply_user` (Boolean) Allow users to **select credentials** from their vault.
- `audio_servername` (String) **PulseAudio server name** for VNC audio.
- `clipboard_encoding` (String) **Clipboard encoding** for the VNC connection. Must be one of: `UTF-8` (default), `UTF-16`, `ISO8859-1`, `CP1252`.
- `color_depth` (Number) **Color depth** in bits per pixel for the VNC connection.
- `cursor` (String) **Cursor rendering mode** for the VNC connection. Must be `local` or `remote`.
- `dest_host` (String) **Destination host** for the VNC connection.
- `dest_port` (Number) **Destination port** for the VNC connection.
- `disable_copy` (Boolean) Whether **copy** is disabled for the connection.
- `disable_paste` (Boolean) Whether **paste** is disabled for the connection.
- `enable_audio` (Boolean) Enable **audio** for the VNC connection.
- `force_lossless` (Boolean) Force **lossless compression** for the VNC connection.
- `read_only` (Boolean) Whether the connection is **read-only**.
- `recording_include_keys` (Boolean) Whether to include **keystrokes** in the recording.
- `rotate_on_termination` (Boolean) **Rotate** launch credentials upon session termination.
- `session_recording` (Boolean) Whether **session recording** is enabled.
- `sftp` (Block, Optional) **SFTP** settings for the VNC connection. (see [below for nested schema](#nestedblock--pam_settings--connection--vnc--sftp))
- `swap_red_blue` (Boolean) **Swap red and blue** color components in the VNC display.

<a id="nestedblock--pam_settings--connection--vnc--sftp"></a>
### Nested Schema for `pam_settings.connection.vnc.sftp`

Optional:

- `enable_sftp` (Boolean) Whether **SFTP** is enabled.
- `sftp_directory` (String) **SFTP root directory**.
- `sftp_resource_uid` (String) UID of the **SFTP resource** record.
- `sftp_server_alive_interval` (Number) **SFTP server alive interval** in seconds.
- `sftp_user_uid` (String) UID of the **SFTP user** record.




<a id="nestedblock--pam_settings--tunnel"></a>
### Nested Schema for `pam_settings.tunnel`

Optional:

- `enable` (Boolean) Whether **tunneling** is enabled for this PAM record.
- `local_port` (Number) **Local port** for tunneling. Only applicable when tunneling is enabled.
- `re_use_port` (Boolean) Whether to **reuse the port** for tunneling. Only applicable when tunneling is enabled.
- `remote_target_port` (Number) **Remote target port** for the tunnel. Only applicable when tunneling is enabled.
- `use_specified_local_port` (Boolean) Whether to use a **specified local port** for tunneling. Only applicable when tunneling is enabled.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import is supported. Import ID is the vault record UID of the PAM machine record.
terraform import commander_new_pam_machine.example "AbCdEfGhIjKlMnOpQrStUw"

# Or use the import block in configuration:
# import {
#   to = commander_new_pam_machine.example
#   id = "AbCdEfGhIjKlMnOpQrStUw"
# }
#
# After import, run terraform plan and align configuration with remote state
# (title, hostname_or_ip, pam_settings, share, etc.).
```
